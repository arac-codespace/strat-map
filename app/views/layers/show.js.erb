// Add the dialog title
$('#myModalLabel').html("<i class='glyphicon glyphicon-pencil'></i> <strong>Fossil Content:</strong> <%=@layer.name%>"); 

// Render the new form
$('.modal-body').html('<%= j render("fossil_show") %>');
 
// Show the dynamic dialog
$('#dialog').modal("show");