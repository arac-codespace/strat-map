<input id="pac-input" class="controls form-control" type="text" placeholder="Search Box">

<div id="map"></div>

<div class="svg_render_container">
  <% @textures_to_render.uniq.each do |i| %>
    <%= render i %>
  <% end %>
</div>

<div class="column-preview text-center" data-avgminthickness = "<%= @avg_min_thickness%>">
  <p>Column Preview</p>

  <button class="btn btn-default btn-xs" data-toggle="collapse" data-target="#help">Help</button>
  <div id="help" class="collapse help-prompt sort-help">
    <p> Hover over the markers to view the data point's name, click it to display a simplified column 
    	of the corresponding data point. A maximum of eight columns can be displayed at the same time. 
    </p>
    <p> Scroll or use the left/right arrow keys to view the columns that extend offscreen. 
    </p>
    <p>
      <strong>Note:</strong> These visualizations don't share the same scale. Create a collection for 
      scaled visualizations.
    </p>
  </div>  

</div>
<div class="flex-container-map tooltip-hover-aid">
<!-- Check maps.js if you decide to modify number of columns -->
</div>