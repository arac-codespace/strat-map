// Add the dialog title
$('#myModalLabel').html("<i class='glyphicon glyphicon-pencil'></i> Edit Column Details "); 

// Render the new form
$('.modal-body').html('<%= j render("column_form") %>');
 
// Show the dynamic dialog
$('#dialog').modal("show");